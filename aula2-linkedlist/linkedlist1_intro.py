"""
Uma lista encadeada não possui índices.

Cada valor dentro dessa lista está próximo um do outro na memória.

Uma lista encadeada possui:
- nós (nodes), que apontam pro próximo elemento da lista (aponta pra None se último.)
- variável cabeça (head) que aponta pro primeiro elemento da lista
- variável cauda (tail) que aponta pro último elemento da lista
"""